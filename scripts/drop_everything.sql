-- Retired destructive helper.
-- Do not run this file against a linked project.
-- Use `supabase db reset --local` for a disposable local database, or create a
-- fresh Supabase project for environment resets. The Supabase CLI migration
-- ledger and managed schemas cannot be safely truncated with this script.
\echo 'drop_everything.sql is retired; use a disposable local reset or a fresh project.'
