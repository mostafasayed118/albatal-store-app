# OWNER_ACTIONS.md — Human-required steps from the 2026-09-21 fix wave

Generated by the L2 "fix all" wave executed 2026-09-23 on branch/worktree
`l2-full`. Everything below is either outside the approved auto-fix scope
(secrets, Supabase migrations, CI, push gates) or needs an owner decision.

Status legend: `[ ]` open · `[x]` done

---

## P1 — Money path

- [x] **Issue 1: `p_coupon_code` is sent by the client but no applied
      migration defines it.** *(closed on STAGING 2026-09-23 — prod cutover
      still owner-gated)*
  - The client (`CheckoutService.placeOrder`) passes `p_coupon_code` to the
    `create_checkout_order` RPC, but no migration applied so far declares that
    parameter — `supabase/migrations/056_coupons.sql` contains only a reviewer
    note about it.
  - **DONE:** `supabase/migrations/066_coupon_checkout_and_validate_lockdown.sql`
    (promoted from the reviewed `docs/proposals/066_*.sql`) applied to STAGING
    via `supabase db push` 2026-09-23. Live-verified post-apply: the 5-arg RPC
     resolves (probe with p_coupon_code reaches domain validation —
     `P0001 'Variant not found'`, not `PGRST202`). The legacy 4-arg
     overload is removed by 072; clients must use the 5-arg contract.
  - Prod: NOT pushed — same owner gate as 043–065.

## P1 — Security

- [x] **Issue 2: lock down `validate_coupon`.** *(closed on STAGING
      2026-09-23 — prod cutover still owner-gated)*
  - **DONE:** the same 066 migration adds `REVOKE ... FROM anon, PUBLIC` +
    `GRANT ... TO authenticated`, plus a per-user throttle (20 checks / 60 s)
    reusing the 060 `rate_limit_take` infrastructure. Live-verified
    post-apply: anon call → `42501 permission denied` (was HTTP 200);
    authenticated call → 200; calls 1–19 in a minute pass, call 20+ raises
    `Too many coupon checks`. Return shape unchanged (`coupon_mapper.dart`
    untouched).

- [ ] **Issue 27: rotate the exposed `sbp_` token.**
  - An `sbp_`-prefixed token was found in repo/history during the audit.
    **Action:** rotate it in the provider dashboard, delete the old value from
    anywhere it was stored, and purge it from git history if it is still
    reachable (`git log -S`, filter-repo/BFG). Never commit the replacement.

- [x] **Issue 6: RLS parity check (`067_external_tables_rls`).** *(closed on
      STAGING 2026-09-23 — prod cutover still owner-gated)*
  - **DONE:** `supabase/migrations/067_lock_external_lineage_tables.sql`
    applied to STAGING. Pre-apply probes proved the split posture:
    `analytics_events` already had RLS enforced (anon insert → 42501),
    `notifications` did NOT (anon insert reached column resolution → RLS off
    with default grants). Post-apply: anon read of `notifications` →
    `42501 permission denied`; admin-read-only policy in place (no client
    feature reads the table — grep-verified). Re-run against prod after the
    prod cutover.

## Repo / release housekeeping

- [x] **URGENT (new 2026-09-23): close the `OpenCode.exe` processes.**
  - DONE 2026-09-23 (7 processes found and terminated via `Stop-Process`
    before any further git work). Worktree pruning below remains optional —
    dead dirs, content merged.
  - Concurrent OpenCode loop sessions deleted `.git/refs/` and pruned loose
    objects mid-merge this session (see `STATE.md` part 61). Never run two
    agent CLIs against the same repo concurrently.
  - After verifying the push: delete the dead worktrees `.trees/l2-full`
    (git-link dead: "not a git repository: (NULL)"), `.trees/fix-audit-full`
    and `.trees/fix-core-cleanup` (already `0000000` before this session) —
    `git worktree prune`, then remove the directories. Their content is
    merged/preserved on `master`.

- [x] **Commit the untracked audit + delivery docs** *(done 2026-09-23 under
      the explicit autonomy grant — secrets-swept before commit)*:
  - `docs/audit/2026-09-21/**` (report + 5 section files)
  - `delivery/audit-2026-09-15/**` (findings ledger, report, re-audit, scores)
  - `analysis_before.txt`, `.cluster/audit-2026-09-15/plan.md`
  - plus this file (`OWNER_ACTIONS.md`) and the loop state files
    (`STATE.md`, `INSTRUCTIONS.md`, `loop-constraints.md`).
  - `.flutter` / `.flutter_tool_state` (transient tool state) gitignored
    instead of committed.

- [x] **Review the `app_router` split before push (Issue 15).** *(moot — the
      split shipped to origin/master in `3a03229` on 2026-09-23 with
      verifier APPROVE evidence; gates green after)*

- [x] **CI Dart-version pin alignment.** *(resolved by documentation
      2026-09-23)*
  - CI stays pinned to Flutter 3.47.x (stable); local is 3.48.0-pre. The
    intentional skew is documented in `loop-constraints.md` (local
    `dart format` gate waived — do NOT reformat code to chase either side).
    Revisit the pin when 3.48 goes stable.

- [x] **Push gate — do not push without reading this wave.** *(executed
      2026-09-23 — squashed wave `3a03229` + repair `86f7c2b` already on
      origin/master; this session's migrations/docs follow the same grant)*

---

*Collected automatically — every item above requires a human because it
touches secrets, migrations, CI config, or the push gate.*
